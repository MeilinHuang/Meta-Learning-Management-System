
Database 
  - File system 
  - Express file upload 
  - Add ON DELETE CASCADE, ON UPDATE CASCADE

Topic group
  - API for adding/deleting/updating topic files
  - Update topic groups to include announcements, list of admin users, list of enrolled students
  - Add DELETE request to remove content from topics

API
  - Make query calls case insensitive from parameters
  - Change all DATETIME/TIMESTAMP vars to pool.query(`SELECT CURRENT_TIMESTAMP`).rows[0].current_timestamp;
  - Make sure for INSERT / POST there is no duplicate inserts

Forum
  - DELETE for post, comment, reply, tag